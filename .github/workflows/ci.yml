name: CI

on:
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      browser:
        description: 'Browser for UI tests'
        required: true
        default: 'Firefox'
        type: choice
        options:
          - Chrome
          - Firefox

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore
        
      - name: Ensure Logs directory exists
        run: mkdir -p Logs

      - name: Run API tests
        run: dotnet test --configuration Release --no-build --filter "Category=API" --logger "console;verbosity=detailed"

      - name: Locate generated log files
        run: find . -type f -name "*.log" -o -name "*.txt"

      - name: Upload API logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: api-test-logs
          path: |
            Logs/
            **/Logs/**/*
          if-no-files-found: warn
          retention-days: 7
 
      - name: Upload API test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: api-test-results
          path: TestResults/api-results.trx

  ui-tests:
    needs: api-tests
    if: always()
    runs-on: ubuntu-latest
    env:
      BROWSER: ${{ github.event.inputs.browser || 'Firefox' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Ensure directories exist
        run: |
          mkdir -p Logs
          mkdir -p TestResults/artifacts/debug/Screenshots

      - name: Run UI tests
        run: xvfb-run dotnet test --configuration Release --no-build --filter "Category=UI" --logger "trx;LogFileName=ui-results.trx" --logger "console;verbosity=detailed" --results-directory TestResults/

      - name: Upload UI test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ui-test-results
          path: TestResults/ui-results.trx

      - name: Upload debug captures
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ui-debug-captures
          path: TestResults/artifacts/debug/
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload UI logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ui-test-logs
          path: Logs/
          if-no-files-found: ignore
          retention-days: 7
